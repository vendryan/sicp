; Because at each computation there is an error
; And that equivalent equation compute differently
; and hence it's different

; maybe we need to make a fraction package
; represent interval as fraction that have numerator and
; denominator and when we need it divide
; numer with denom. This way it's accurate and
; Representing integer is easier for computer than 
; representing floating point
